import React from 'react';
import { View, Text } from 'react-native';

export default function AnimalDetailsImagesCarousel(props) {
  const { animal } = props;

  return (
    <View>
      <Text>AnimalDetailsImagesCarousel</Text>
    </View>
  );
}
